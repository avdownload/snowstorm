syntax = "proto3";
package song;

// [RUST-SIGNAL]
message Song {
    string title = 1;
    string album = 2;
    string artist = 3;
}

// [RUST-SIGNAL]
message Songs {
    repeated Song songs = 1;
}
